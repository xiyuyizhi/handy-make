{
  "packages": [
    "packages/*"
  ],
  "version": "1.0.0",
  "command": {
    "publish": {
      "message": "(release): publish %s",
      "ignoreChanges": [
        "ignored-file",
        "*.md"
      ]
    }
  }
}
